---
const { src, alt = 'Banner' } = Astro.props;
---
<section class="relative w-full">
  <div class="relative w-full h-[35vh] min-h-[280px] max-h-[400px]">
    <img src={src} alt={alt} class="absolute inset-0 w-full h-full object-cover" />
    <div class="absolute inset-0 bg-gradient-to-r from-black/40 to-black/20"></div>
    <div class="relative z-10 flex items-center h-full">
      <div class="max-w-6xl mx-auto px-6 text-white">
        <h1 class="text-2xl md:text-4xl font-bold mb-2">Tu hogar, tu estilo.</h1>
        <p class="text-base md:text-lg opacity-95">Promos y colores que cambian cada mes.</p>
      </div>
    </div>
  </div>
</section>