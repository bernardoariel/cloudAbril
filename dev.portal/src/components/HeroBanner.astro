---
const highlightBadges = [
  'Diseno argentino',
  '12 cuotas sin interes',
  'Entrega en 72 hs'
];
---
<section class="relative overflow-hidden">
  <div class="mx-auto flex w-full max-w-6xl flex-col gap-10 px-4 pb-16 pt-10 lg:flex-row lg:items-center lg:pb-24 lg:pt-16">
    <div class="flex-1 space-y-6">
      <div class="flex items-center gap-3 text-xs font-semibold uppercase tracking-[0.4em] text-[color:var(--accent)]">
        <span class="h-px w-5 bg-[color:var(--accent)]"></span>
        Abril Amoblamientos
      </div>
      <h1 class="text-4xl font-semibold text-[color:var(--text)] sm:text-5xl lg:text-6xl">
        Disena cada ambiente con piezas creadas para durar
      </h1>
      <p class="max-w-xl text-base text-[color:var(--text-muted)] sm:text-lg">
        Catalogos mensuales, terminaciones premium y asesoramiento experto para renovar tu casa u oficina con identidad propia.
      </p>
      <div class="flex flex-wrap gap-3">
        {highlightBadges.map((badge) => (
          <span class="rounded-full bg-[color:var(--accent-muted)] px-4 py-2 text-xs font-semibold text-[color:var(--brand)]">
            {badge}
          </span>
        ))}
      </div>
      <div class="flex flex-wrap items-center gap-4 pt-4">
        <a
          class="button-ring rounded-full bg-[color:var(--brand)] px-6 py-3 text-sm font-semibold uppercase tracking-wide text-white shadow-lg transition hover:bg-[color:var(--accent)]"
          href="#ofertas"
        >
          Ver ofertas
        </a>
        <a class="flex items-center gap-2 text-sm font-semibold text-[color:var(--brand)]" href="/productos">
          Ver catalogo completo
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13.5 4.5L21 12l-7.5 7.5M21 12H3" />
          </svg>
        </a>
      </div>
    </div>
    <div class="flex-1">
      <div class="relative w-full overflow-hidden rounded-3xl border border-white/60 bg-gradient-to-br from-[color:var(--brand)]/20 via-white to-[color:var(--accent-muted)] shadow-2xl">
        <div class="aspect-[2193/827] w-full">
          <img
            src="https://images.unsplash.com/photo-1505691723518-36a5ac3be353?auto=format&fit=crop&w=1400&q=80"
            alt="Living moderno con muebles de Abril Amoblamientos"
            class="h-full w-full object-cover"
            loading="lazy"
          />
        </div>
        <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/30 via-transparent to-transparent"></div>
        <div class="absolute bottom-4 left-4 flex flex-wrap gap-2">
          <span class="rounded-full bg-white/80 px-3 py-1 text-xs font-semibold uppercase text-[color:var(--brand)]">2193 x 827</span>
          <span class="rounded-full bg-white/80 px-3 py-1 text-xs font-semibold uppercase text-[color:var(--brand)]">Coleccion Abril 2025</span>
        </div>
      </div>
    </div>
  </div>
</section>

